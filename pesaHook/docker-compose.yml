services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: pesahook
      POSTGRES_PASSWORD: devpass
      POSTGRES_DB: pesahook
    ports:
      - "5432:5432"
    volumes:
      - pesahook_pgdata:/var/lib/postgresql/data

volumes:
  pesahook_pgdata:
